# Supabase Configuration
# Get these values from your Supabase project settings:
# 1. Go to https://supabase.com/dashboard
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the Project URL and anon/public key

# Supabase project URL (publicly accessible)
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Supabase anonymous/public key (publicly accessible)
# This key is safe to use in browser as it has Row Level Security (RLS) restrictions
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key (server-only, keep secret!)
# Only use this in server-side code or API routes
# This key bypasses Row Level Security - never expose to the client
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
